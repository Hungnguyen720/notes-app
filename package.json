{
  "name": "notes-app",
  "version": "1.0.0",
  "private": true,
  "description": "A small browser-based notes application with local persistence.",
  "engines": {
    "node": "22.14.0"
  },
  "packageManager": "npm@10.9.2",
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --project jsconfig.json",
    "test": "node --test",
    "build": "node scripts/build.mjs",
    "verify": "npm run lint && npm run typecheck && npm run test && npm run build"
  },
  "devDependencies": {
    "eslint": "10.9.1",
    "jsdom": "29.1.1",
    "typescript": "7.0.2"
  }
}
